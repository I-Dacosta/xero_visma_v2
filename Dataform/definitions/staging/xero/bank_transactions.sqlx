config {
  type: "declaration",
  schema: "staging_xero",
  name: "bank_transactions",
  description: "Staging: Xero bank transactions header (SPEND/RECEIVE). One row per tenant_id + bank_transaction_id. Populated outside Dataform by the Python ETL."
}
