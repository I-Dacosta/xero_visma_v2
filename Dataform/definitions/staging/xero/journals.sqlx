config {
  type: "declaration",
  schema: "staging_xero",
  name: "journals",
  description: "Staging: Xero general ledger journal headers (/Journals — the GL source of truth; every document type posts here, tagged by source_type, e.g. ACCREC, ACCPAY, MANJOURNAL). One row per tenant_id + journal_id. Populated outside Dataform by the Python ETL (etl/xero/journals.py)."
}
