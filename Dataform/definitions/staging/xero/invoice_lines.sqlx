config {
  type: "declaration",
  schema: "staging_xero",
  name: "invoice_lines",
  description: "Staging: Xero invoice line items (unnested from invoices LineItems[]). One row per tenant_id + line_item_id. Carries up to 2 tracking category/option pairs. Populated outside Dataform by the Python ETL."
}
