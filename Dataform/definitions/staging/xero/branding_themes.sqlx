config {
  type: "declaration",
  schema: "staging_xero",
  name: "branding_themes",
  description: "Staging: Xero branding themes (invoice templates). One row per tenant_id + branding_theme_id. Populated outside Dataform by the Python ETL."
}
