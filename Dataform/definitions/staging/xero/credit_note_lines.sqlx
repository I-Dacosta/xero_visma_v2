config {
  type: "declaration",
  schema: "staging_xero",
  name: "credit_note_lines",
  description: "Staging: Xero credit note line items (unnested from credit_notes LineItems[]). One row per tenant_id + line_item_id. Carries up to 2 tracking category/option pairs. Populated outside Dataform by the Python ETL."
}
