config {
  type: "declaration",
  schema: "staging_xero",
  name: "overpayment_lines",
  description: "Staging: Xero overpayment lines (LineItems[] on /Overpayments) — the account-level grain a GL reconciliation needs; the header only carries the total. One row per tenant_id + record_id + line_item_id. Populated outside Dataform by the Python ETL (etl/xero/overpayments.py)."
}
