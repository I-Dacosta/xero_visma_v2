config {
  type: "declaration",
  schema: "staging_xero",
  name: "credit_notes",
  description: "Staging: Xero credit notes header (ACCRECCREDIT + ACCPAYCREDIT). One row per tenant_id + credit_note_id. Populated outside Dataform by the Python ETL."
}
