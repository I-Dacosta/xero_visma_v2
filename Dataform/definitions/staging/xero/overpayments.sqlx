config {
  type: "declaration",
  schema: "staging_xero",
  name: "overpayments",
  description: "Staging: Xero overpayments (/Overpayments — money received/paid beyond what was owed, before allocation to a future invoice). Header only. One row per tenant_id + overpayment_id. Restored 2026-07-24 to close the journals.source_type IN ('APOVERPAYMENT','AROVERPAYMENT') GL-reconciliation gap. Populated outside Dataform by the Python ETL (etl/xero/overpayments.py)."
}
