config {
  type: "declaration",
  schema: "staging_xero",
  name: "tracking_options",
  description: "Staging: Xero tracking options (values within a tracking category). One row per tenant_id + tracking_option_id. Populated outside Dataform by the Python ETL."
}
